options:
  logging: GCS_ONLY

logBucket: $_LOG_BUCKET

steps:
  - name: 'node:20'
    entrypoint: bash
    args:
      - -c
      - |
        npm install
        npm run build

  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - app
      - deploy
      - '--version=$_VERSION'
      - '--quiet'
